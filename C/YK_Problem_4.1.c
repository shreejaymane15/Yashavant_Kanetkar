/*A year is entered through the keyboard, write a program to determine the year is leap or not. Use the logical operators && and ||*/
#include<stdio.h>
int main(){
    int year, leap;
    printf("/n Enter Year = ");
    scanf("%d",&year);
}